import 'package:flutter/material.dart';

class GYProfilePage extends StatelessWidget {
  const GYProfilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.blue,);
  }
}
