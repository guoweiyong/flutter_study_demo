import 'package:flutter/material.dart';

class GYSubjectPage extends StatelessWidget {
  const GYSubjectPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.orange,);
  }
}
